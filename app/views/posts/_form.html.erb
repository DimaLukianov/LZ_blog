<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
 
  <%= f.label :category %>
 
  <%= select_tag("category_ids", options_for_select(Category.all.collect { |category| [category.category_name, category.id] }, @post.categories.collect { |category| category.id})) %>
 
</div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text, size: '10x25'%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
